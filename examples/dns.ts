// import { ACMEClient, directory } from '../src/index.ts';

// const client = new ACMEClient(directory.letsencrypt.staging.directoryUrl);

// const domain = await input({ message: 'Enter domain name:' });

// console.log(`Creating order for domain: ${domain}`);
